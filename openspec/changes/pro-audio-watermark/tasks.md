# Tasks

## 1. Config and options
- [ ] 1.1 Stamp config (source URI resolved via existing types, boot validation, content identity captured for the cache key), `wm:`/`wm_gain:` options with bounds
## 2. Render
- [ ] 2.1 Interval overlay via the shared filter machinery; one process, one slot
## 3. Tests
- [ ] 3.1 Interval presence in rendered output; gain applied; not-configured error path
- [ ] 3.2 Cache-key includes stamp identity; swap-and-restart serves fresh variants
## 4. Docs
- [ ] 4.1 PRO contract + the screener recipe (wm + exp together)
