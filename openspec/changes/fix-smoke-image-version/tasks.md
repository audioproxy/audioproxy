## 1. Fix

- [ ] 1.1 Read the version from `@version "…"` in `mix.exs`, and raise with a clear message when no version is found
- [ ] 1.2 Run `bin/smoke-image` locally and confirm that every check passes, `OCI version label is set` included
